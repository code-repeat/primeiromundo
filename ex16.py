from math import trunc
num = float(input('Digite um número real qualquer: '))
num_quebrado = trunc(num)
print(num_quebrado)

#  poderia usar apenas a função int() para deixar o valor inteiro
#  sem a necessidade de importar módulos
